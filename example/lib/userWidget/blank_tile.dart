import 'package:flutter/material.dart';

class BlankTile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Blank Tile'),
      ),
    );
  }
}